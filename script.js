/* Retro landing page JS (no deps) */
(() => {
  const $ = (id) => document.getElementById(id);

  const els = {
    stars: $("stars"),
  };

  // Canvas removed - using CSS gradient background instead

  // All animations disabled - elements are static
})();


